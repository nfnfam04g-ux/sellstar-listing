# セルスタ出品ツール 運用ガイド

最終更新: 2026-06-08 ／ 対象: 「再出品ツール」スプレッドシート × セルスタ（sellsta.jp）

> このガイドは**使う人向けの実践マニュアル**です。設計の根拠・全エラーコード等の細部は
> 正本仕様書 [`出品ツール仕様書.md`](出品ツール仕様書.md)（付録C＝操作正本／付録E＝エラーコード）を参照してください。

---

## 0. これは何をする道具か（30秒で）

リサーチ担当が記入した**スプレッドシート1行**を読み取り、在庫管理ツール**セルスタの新規出品画面へ自動で転記**し、
**「下書き保存」まで**を行います。

- ✅ やること: 新規出品 → タイトル/SKU/価格/コンディション/Shipping Policy/商品説明(HTML)/利益計算 を転記 → **下書き保存**
- 🚫 やらないこと: **最終の「出品」ボタンは絶対に押しません**。人が下書きを目視チェックしてから出品ボタンを押します。

```
[スプレッドシート1行] ──読取(gspread)──▶ [このツール] ──転記(Claude in Chrome)──▶ [セルスタ 下書き保存]
                                              │
                                              └──結果を書き戻し──▶ [出品ステータス列 / 出品エラー列]
```

---

## 1. 仕様の詳細

### 1-1. 処理の全体フロー（1行あたり）
1. **対象行を取得** — 「出品ステータス」列が**空欄**かつ「ItemURL」が入っている行＝未処理。
2. **ロック** — 着手と同時に出品ステータス列へ `処理中` を書く（二重処理・他PCとの競合を防止）。
3. **必須チェック** — 必須項目の欠落を判定（足りなければ `スキップ` / `要確認`）。
4. **セルスタへ転記** — 新規出品モーダル → 出品画面入力 → 利益計算 → **下書き保存**。
5. **書き戻し** — 成功なら出品ステータス＝`下書き保存`。失敗なら区分＋`出品エラー`列にコード＋ログ。
6. 次の行へ。

### 1-2. 3つの実行モード
| モード | コマンド | 何をするか |
|---|---|---|
| **手動（既定）** | `./sellstar.sh run` | 未処理行を上から順に下書き保存まで処理（対話・画面を見ながら） |
| **単発** | `./sellstar.sh run 9` | 指定した1行（例:9行目）だけ処理 |
| **自動巡回** | `./sellstar.sh patrol` | 無人で1巡回ぶん処理。スリープ防止つき。launchdで3時間ごとに自動実行も可 |

### 1-3. シートの読み方（列はヘッダー名で解決）
列レター（B列等）を**固定で決め打ちしません**。毎回1行目のヘッダー名から列位置を引くので、
**列を移動・挿入しても壊れません**。主な対応:

| 用途 | ヘッダー名 | 備考 |
|---|---|---|
| 出品ステータス（状態書込先） | `出品ステータス` | 空=未処理 / `処理中`/`下書き保存`/`スキップ`/`リトライ`/`要確認` |
| 出品エラー（コード＋ログ） | `出品エラー` | 例: `E302 ...（Shipping Policy）2026-06-08 14:03` |
| 出品ストア | `ストア名` | 新規出品「出品ストア選択」へ（例 mionastore307） |
| カスタムラベル(SKU) | `仕入れキーワード` | eBay の Custom label(SKU) 欄 |
| eBay商品URL | `ItemURL` | 新規出品モーダルへ**フルURLのまま**入力 |
| 仕入先URL/種別 | `仕入元URL①②③` / `仕入元①②③` | メルカリ/楽天/Yahoo!フリマ等 |
| タイトル | `タイトル` | Item title |
| コンディション元 | `状態` (／中古英文は `状態テンプレ`) | 別シート「状態　テンプレ」A列→C列で英文化 |
| 商品説明（完成HTML） | `Description` | Summernote の CodeView にそのまま流し込む |
| 売値 | `売値($)` | Selling Price |
| Shipping Policy | `Shipping Policy` | 出品画面で**同名**を選択 |
| ベストオファー | `Best offer` / `拒否価格` | ON時のみ。拒否価格を入力、承認価格は触らない |
| 利益計算 | `実重量(g)`/`高さ(cm)`/`奥行き(cm)`(=長さ)/`幅(cm)`/`リサーチ担当` | 値があるものだけ入力。メモ＝担当者名 |
| カスタムリンク | `検索URL` | |

### 1-4. セルスタ転記の中身
1. **新規出品モーダル**: 出品ストア選択（`ストア名`）→ eBay商品URL（`ItemURL`）→ 仕入先URL/種別 → 出品テンプレート（`発送方法`で `Economy`/`DHL fedex` を分岐）→「出品画面へ」。
2. **出品画面入力**: タイトル / カスタムリンク / SKU / コンディション / Selling Price / ベストオファー / **Shipping Policy** / 商品説明(HTML)。
   - コンディションが **New** のときは Condition description は不要（触らない）。中古のときだけ英文を入れる。
   - 商品説明は `Description` 列の完成HTMLを **Summernote の `</>`（CodeView）** からそのまま投入。
3. **利益計算**: 重量・寸法を入力 → メモにリサーチ担当名 → 利益計算保存 → 出品画面へ戻る。
4. **下書き保存**（←ここで止める）。

### 1-5. 安全ルール（最重要）
- **最終の「出品」ボタンは絶対に押さない。** ゴールは常に「下書き保存」。
- 着手直後に `処理中` を書いてロック。`処理中`/`下書き保存`/`出品済` の行は処理しない。
- 入力後は画面値を**読み戻して確認**（read-back）。
- 不確実なら止めて `要確認` を記録（曖昧なまま進めない）。

### 1-6. エラー処理（3層 × 4区分）
記録は **3層**に分離します（詳細は [`reference/error-codes.md`](.claude/skills/sellstar-listing/reference/error-codes.md)）:
1. **出品ステータス列** … 区分の日本語だけ（`下書き保存`/`スキップ`/`リトライ`/`要確認`）
2. **出品エラー列** … `コード 一言理由（項目）日時`
3. **セルスタのメモ欄** … 同じログを複写

回復**4区分**:
| 区分 | 意味 | 動作 |
|---|---|---|
| `リトライ` | 一時的(通信/UI) | 自動で最大3回 → 超過で `要確認` |
| `スキップ` | 上流データ未完 | 今回は飛ばす（次巡回で再判定） |
| `ハル` | 続行不能(未ログイン/上限) | 巡回停止＋通知 |
| `要確認` | データ不正(人が直す) | 入った分まで下書き保存して停止 |

代表例: **Shipping Policy(CY)が空** → 売値の価格帯に合う暫定ポリシーで下書き保存し `要確認`＋`E302`（人が後で正す。ブロックしない）。

---

## 2. 使い方

### 2-(a) ターミナルに貼るコマンド

> 初回・サブPCでは、まず **`doctor`（環境チェック）** を実行してください。全部 ✅ になればOKです。

```bash
# ① ツールの場所へ移動（以後このディレクトリで作業）
cd "/Users/nfnfa/company/eBay/ebay-listing/sellstar-listing"

# ② 環境チェック（venv・認証・シート疎通・未処理件数）
./sellstar.sh doctor

# ③ 未処理行を一覧
./sellstar.sh pending

# ④ 中身を確認（例: 9行目）
./sellstar.sh read 9

# ⑤ 出品作業を実行
./sellstar.sh run          # 手動モード：未処理行を上から順に
./sellstar.sh run 9        # 単発モード：9行目だけ

# ⑥ 自動巡回（無人・1巡回）
./sellstar.sh patrol

# 手書きでステータスを直したいとき（任意）
./sellstar.sh status 9 下書き保存
./sellstar.sh error  9 "E302 Shipping Policy要確認（Shipping Policy）2026-06-08 15:00"
```

別の場所からでも、フルパスで叩けます:
```bash
"/Users/nfnfa/company/eBay/ebay-listing/sellstar-listing/sellstar.sh" pending
```

**事前条件（run/patrol を使う前に）**: Chrome でセルスタ（sellsta.jp）に**ログイン済み**、かつ
**Claude in Chrome 拡張が接続済み**であること。

### 2-(b) ファイルの保存場所

**保存先（正本）**: `/Users/nfnfa/company/eBay/ebay-listing/sellstar-listing/`（eBayツール群と同じ場所）

| 役割 | パス（sellstar-listing/ からの相対） |
|---|---|
| フォルダ案内 | `README.md` |
| ランチャー（人が叩く入口） | `sellstar.sh` |
| この運用ガイド | `運用ガイド.md` |
| 正本仕様書 | `出品ツール仕様書.md` |
| 商品説明HTML構造サンプル | `description-template-sample.html` |
| スキル本体フォルダ | `.claude/skills/sellstar-listing/` |
| ├ スキル本文（操作手順） | `.claude/skills/sellstar-listing/SKILL.md` |
| ├ ランチャー実体 | `.claude/skills/sellstar-listing/sellstar.sh` |
| ├ シート読み書き | `.claude/skills/sellstar-listing/sheet_io.py` |
| ├ 依存パッケージ一覧 | `.claude/skills/sellstar-listing/requirements.txt` |
| ├ エラーコード表 | `.claude/skills/sellstar-listing/reference/error-codes.md` |
| ├ コンディション対応表 | `.claude/skills/sellstar-listing/reference/condition-map.md` |
| └ 巡回スケジューラ雛形 | `.claude/skills/sellstar-listing/patrol/com.sellstar.patrol.plist` |

| 役割（フォルダ外） | パス |
|---|---|
| 認証JSON（サービスアカウント） | `/Users/nfnfa/Desktop/credentials.json/credentials.json` |
| Python仮想環境(venv) | `/Users/nfnfa/Desktop/credentials.json/venv/` |
| 巡回ログ | `~/Library/Logs/sellstar-patrol.log` |

対象スプレッドシート「再出品ツール」: ID `1XNwsNA8x6v06-rfGy1T02DAsSrmXJc1zUVZ_wmXZRTM` / タブ「出品」（1〜4行目ヘッダー、データは5行目以降）。

### 2-(c) 具体的な操作手順

**A. 手動モード（おすすめ・画面を見ながら）**
1. Chrome でセルスタにログインしておく。
2. `cd "/Users/nfnfa/company/eBay/ebay-listing/sellstar-listing" && ./sellstar.sh doctor` で ✅ を確認。
3. `./sellstar.sh run` を実行 → Claude Code が起動し、未処理行を順に転記して下書き保存。
4. 各行が `下書き保存` になったらセルスタの下書きを目視 → 問題なければ**人が「出品」を押す**。

**B. 単発モード（1行だけ試す／特定行をやり直す）**
1. `./sellstar.sh read 9` で内容を確認。
2. `./sellstar.sh run 9` で9行目だけ処理。

**C. 自動巡回（無人）**
- 単発で回す: `./sellstar.sh patrol`（caffeinate でスリープ防止。ログは `~/Library/Logs/sellstar-patrol.log`）。
- 3時間ごとに自動化する（launchd）:
  ```bash
  cp "/Users/nfnfa/company/eBay/ebay-listing/sellstar-listing/.claude/skills/sellstar-listing/patrol/com.sellstar.patrol.plist" ~/Library/LaunchAgents/
  launchctl load ~/Library/LaunchAgents/com.sellstar.patrol.plist   # 開始
  launchctl start com.sellstar.patrol                               # 今すぐ1回テスト
  launchctl unload ~/Library/LaunchAgents/com.sellstar.patrol.plist # 停止
  ```
  ⚠ 無人巡回は **Chrome がログイン済み＆拡張が接続済み＆画面が起きている**ことが前提です。確実性を重視するなら手動モード推奨。

---

## 3. サブPCとの同期方法

複数台（メイン／サブPC）で同じ仕組みを動かせます。**シートの `処理中` ロック**で排他されるので、
同時に巡回しても二重出品にはなりません。

### 3-1. サブPCに用意するもの（4点）
1. **ツールフォルダ** … `…/.claude/skills/sellstar-listing/`（SKILL.md・sellstar.sh・sheet_io.py・requirements.txt・reference・patrol）
2. **正本仕様書** … `出品ツール仕様書.md`（任意・参照用）
3. **Python仮想環境(venv)** … gspread を入れたもの（下で作る）
4. **認証JSON** … サービスアカウントの `credentials.json`（🔑 秘密鍵。取り扱い注意）

### 3-2. 同期の手順

**(1) ファイルを運ぶ** — どちらか:
- **Git/クラウド同期**: プロジェクト一式（`ebay-first-project/`）をサブPCにも展開する。`.claude/skills/sellstar-listing/` がそのまま使える。
  ※ ただし **`credentials.json` はリポジトリに入れない**（秘密鍵。`.gitignore` 推奨）。別途安全に渡す。
- **手動コピー**: フォルダごとコピー（USB/AirDrop/共有ドライブ）。

**(2) Python環境を作る**（サブPCで1回だけ）:
```bash
cd <任意の安全な場所>            # 例: ~/sellstar
python3 -m venv venv
./venv/bin/python -m pip install -r <skillの場所>/requirements.txt
```

**(3) 認証JSONを安全に置く** — 例 `~/sellstar/credentials.json`。
- 🔑 これはサービスアカウントの**秘密鍵**です。メール本文やチャット・公開リポジトリに置かないこと。
  AirDrop / 暗号化USB / パスワード付き共有など安全な経路で渡す。

**(4) パスが違う場合だけ環境変数で教える**（メインと同じ `/Users/nfnfa/...` なら不要）:
```bash
export SELLSTAR_VENV=/Users/<サブPCのユーザー>/sellstar/venv
export SELLSTAR_CREDENTIALS=/Users/<サブPCのユーザー>/sellstar/credentials.json
# 毎回打つのが面倒なら ~/.zshrc に追記しておく
```

**(5) 動作確認**:
```bash
cd <skillの場所>/sellstar-listing
./sellstar.sh doctor      # 全部 ✅ になればOK
```

**(6) セルスタ側** — サブPCの Chrome でも**セルスタにログイン**し、**Claude in Chrome 拡張を接続**しておく。

### 3-3. シート共有は1回でOK
シートはサービスアカウント（`naka31@enduring-stage-496617-n5.iam.gserviceaccount.com`）に
**「編集者」で共有済み**です。**同じ認証JSONを使う限り、どのPCでも追加共有は不要**です。
（もし `doctor` で読取に失敗したら、この共有が外れていないか確認）

### 3-4. 同時稼働の安全装置
- 着手した行はすぐ `処理中` になり、`pending`（未処理）に出なくなる → 別PCが同じ行を掴まない。
- そのため、メインとサブで同時に `patrol`/`run` しても**重複処理になりません**。
- 可能なら別シート「稼働ログ」に `端末名/日時/件数/結果` を1行残すとハートビートになります（任意）。

---

## 4. 困ったとき（FAQ）

| 症状 | 対処 |
|---|---|
| `doctor` でシート読取が ✗ | 多くは共有もれ。再出品ツールを SA に「編集者」で共有（3-3）。 |
| `gspread 未導入` | `<venv>/bin/python -m pip install -r requirements.txt` |
| `run`/`patrol` で claude が無い | claude CLI を入れる（`~/.local/bin/claude`）。`pending`/`read`/`status` はclaude不要。 |
| 転記が始まらない/止まる | Chrome がセルスタにログイン済みか・拡張が接続済みか確認。`新規出品`モーダルは稀に開き直しが要る。 |
| Shipping Policy が空の行 | 自動で暫定ポリシー＋`要確認`＋`E302`で下書き保存される。人が後で正しいポリシーに直す。 |
| 同じ行を二重に処理しそう | 起きません（`処理中`ロック）。万一 `処理中` で固まった行は手動で空に戻す。 |
| 出品ボタンを押してほしい | ツールは押しません（仕様）。下書きを目視後、人が押してください。 |

---

### 関連ドキュメント
- 正本仕様書: [`出品ツール仕様書.md`](出品ツール仕様書.md)（付録C=操作正本 / 付録E=エラーコード / 付録F=サブPC / 付録G=実機UI）
- スキル本文: [`SKILL.md`](.claude/skills/sellstar-listing/SKILL.md)
- エラーコード一覧: [`reference/error-codes.md`](.claude/skills/sellstar-listing/reference/error-codes.md)
