# eBay 出品ツール 仕様書（ドラフト v1.6）

> v1.6: 通しテスト成功（付録G-6）。確定変更: ①eBay Item URLは**フルURLのまま入力** ②Custom label(SKU)＝**`仕入れキーワード`(E列)**（旧C列SKUから変更） ③Shipping Policyは**`Shipping Policy`(CY)列と同名**を選択（テンプレ任せをやめる）。シート書き戻しは本番で自動運用。
> v1.5: 実機調査を反映（**付録G**）。新規出品モーダル/出品画面/利益計算モーダルの実フィールドを記録。
> 出品テンプレート選択肢は実機では「新品/中古 × DHL/エコノミー価格帯 / 2500ドル以上DDU」等＝C-2想定と相違→選択ルール要確定(G-4)。Condition descriptionは「新品以外のみ記入可」をUIで確認。DescriptionエディタのCodeViewは`</>`ボタン。
> v1.4: 説明文フローを確定。`取得`(ES,日本語)→`英訳`(ET,eBay用HTML)→`ディスクリプション`(Z,テンプレ合体済み完成HTML)。**ツールはZ列をCodeViewに貼るだけ（生成・組み立ては上流）**。⚠Z列の用途が旧「コンディション説明」と食い違い→要確認（Condition description欄の取り込み元）。
> v1.3: テンプレ使い分け（価格 $2,500 でテンプレ1/2＝米国関税文言、発送方法 Economy/DHL で Payment/Shipping 文言）は **スプレッドシート側で振り分け済み＝ツール作業外**に確定。ツールはシートが用意した正しいHTMLを使うだけ。出品ストア名は必ず A列から選択。セルスタは「出品テンプレート＋DHL FedEx」を選択（説明文HTMLとは別概念）。
> v1.2: 説明文（Description）方式を確定（第7-2）。ユーザー提供の完成HTML実例を `description-template-sample.html` に保存し正本化。**固定の枠＋②可変本文の2層／生成プロンプトは②本文HTML断片のみ生成／CodeView貼付**。
> v1.1: 起動方法を整理（①ターミナル合図 ②ワンクリック ③スケジューラ）。**段階導入＝まず①で動作確認 → 安定後に③・②を追加**を確定（付録D-2）。
> v1.0: 旧 `セルスタ出品`(FC)/`セルスタログ`(FD)列を**削除し `出品エラー`列に統合**。エラーコードは一律 `出品エラー`列。RETRY最大3回 確定。全体の列参照を統一。
> v0.9: 巡回を**3時間に1回**に確定。出品対象＝**B列が空の行**を再明記。**全エラーコード一覧（E-8）**を追加。
> v0.8: ステータスを3層分離に確定（区分=B列 / コード=出品エラー列 / 解説=別シート「エラーコード」）。回復4区分（リトライ/スキップ/ハル/要確認）。**付録F サブPC・複数端末運用**を新設。
> v0.7: エラーコード設計・回復ポリシーを **付録E** として追加（工程別採番 / RETRY・SKIP・FAIL 分類 / ステータス2列分離の推奨）。

最終更新: 2026-06-08

> v0.6 追記: **列はヘッダー名で解決**（列移動に耐える）。DDP対応関税率＝「DDP対応関税率」列(FE)（旧PEは誤り）。
> v1.4 訂正: **Z列のヘッダー＝`description`（完成HTML、CodeView貼付対象）**。旧「Z列＝コンディション説明」は誤り。
> eBayの **Condition description 欄**は中古品のときのみ、`状態テンプレート`(X列)→「テンプレート（英語）」で英語化して入力（付録C-3）。
> v0.5: ユーザーが列レター付きで操作手順を確定指示。**付録C（確定操作手順）が実装の正本**。
> v0.4: 実出品用スプレッドシート（再出品ツール）を解析。工程管理列・配送ポリシー価格帯表・
> コンディション参照表・テンプレHTML・画像取得スクリプトの実態を反映（付録A）。
>
> ※状態画像の取得は不要 / カスタムリンクは DI列「検索URL」を使用 / メモの名前は L列「リサーチ担当」。

## 1. 目的・概要

リサーチ外注さんが Google スプレッドシートに入力した商品情報を、在庫管理ツール
**セルスタ（sellsta.jp）** の出品画面に転記し、**「下書き保存」までを自動／半自動で行う**ツール。
最終的な「出品」ボタンは人間（出品担当者）が目視チェック後に押す。

- 1 行 = 1 商品
- スプレッドシートは **完成済み**（タイトル・説明文・スペック・状態・画像はすべて上流ツールで確定済み）
- 出品ツールは「完成した 1 行を読んでセルスタに入力する」役割に専念する（疎結合設計）

## 2. ツール形態

- **方式: Claude in Chrome ベース**（既存の `sellstar-sync` スキルと同じ仕組み）
- 新規ソフトのインストール不要。ブラウザを Claude が操作してシート → セルスタに入力。
- 「入力 → 下書き保存 → 人が目視チェック → 出品ボタンを人が押す」運用に素直に乗る。

### 運用モード（両対応）
| モード | 説明 | トリガ |
|---|---|---|
| **自動（巡回）** | サブ PC を 24h 稼働。**3 時間に 1 回**シートを巡回し、未処理行（B列空欄）を下書き保存まで自動化 | スケジュール実行（`/loop` 等） |
| **手動** | 出品担当者が「この行を出品して」と指示して都度実行。目視チェックしながら | 都度指示 |

- リサーチ外注さんでも操作しやすい形を最終ゴールとする。

## 3. 実行単位（モジュール構成）

各工程を**独立に動かせる**こと、かつ**通しでも動かせる**ことを要件とする。

| 実行単位 | 説明 |
|---|---|
| (a) 行（セル）単位 | 指定した 1 行（or 行範囲）を処理 |
| (b) ツール単位 | 「タイトルだけ」「スペックだけ」等、特定工程のみ実行 |
| (c) 通し実行 | URL入力 → 各項目入力 → 利益計算 → 下書き保存までを一気通貫 |
| (d) 自動巡回 | 未処理行を検出し (c) を自動反復（最終ゴール） |

## 4. 処理対象行の選定とステータス

- `出品ステータス`(B列) が **空欄** かつ `出品URL` or `ItemURL` が入っている行を対象とする。
- 処理開始時に `処理中` にして二重処理を防止（巡回ロック）。下書き保存できたら `下書き済` に更新。

### 4-1. ステータス値の定義（`出品ステータス` B列）
※セルスタ画面側のバッジ（出品中/出品終了/下書き/エラー）とは別の、**シート上の進行管理**用。

| 値 | 意味 | セットする主体 |
|---|---|---|
| （空欄） | 未処理。巡回対象 | リサーチ完了時点 |
| `処理中` | ツールが処理中（二重処理防止ロック） | ツール |
| `下書き済` | 下書き保存完了、人の目視チェック待ち | ツール |
| `出品済` | 担当者が出品ボタンを押した | 担当者 / 出品検知 |
| `エラー` | 失敗。ログをセルに記録（第8章参照） | ツール |

※ さらに工程管理列の **`出品エラー`** 列にエラーコード＋ログを書く（v1.0で旧 `セルスタ出品`/`セルスタログ` を統合・削除）。詳細は付録E。

## 5. スプレッドシート列 → セルスタ マッピング

### 5-1. 新規出品モーダル（URL 入力ステップ）
| セルスタ項目 | シート列 | 処理 |
|---|---|---|
| 出品ストア選択 | **「出品ストア」列**（値は別シート「ストア名」A列から選択） | 5-4 参照 |
| 出品方式: eBay URL/ID | `出品URL`(A) / `ItemURL`(列26) | コピペ |
| 仕入れ先①(情報取得/在庫監視) | `仕入元URL①`(列6) ＋ `仕入元①`(列5) で EC 種別プルダウン | コピペ＋プルダウン |
| 仕入れ先②(在庫監視) | `仕入元URL②`(列8) ＋ `仕入元②`(列7) | コピペ＋プルダウン |
| 仕入れ先③(在庫監視) | `仕入元URL③`(列10) ＋ `仕入元③`(列9) | コピペ＋プルダウン |
| 出品テンプレート選択 | 共通テンプレを選択（第7章） | プルダウン |

### 5-2. 出品画面
| セルスタ項目 | シート列 | 処理 |
|---|---|---|
| Item title | `タイトル`(列20) | コピペ（80字以内。`文字数`列21 で検証） |
| Photos & Videos | （自動読込） | **セルスタが自動取得するため操作不要** |
| Custom Link | **指定セル**（カスタムリンク用） | コピペ |
| Custom label (SKU) | `SKU`(列2) | コピペ |
| Condition | `状態`(列22) を 日本語→英語マッピングで変換（第7章） | プルダウン |
| Condition description | `状態説明`(列24) | コピペ（新品以外のみ） |
| Item Code | `UPC`(列31) / `EAN`(列32) / `MPN`(列33) | コピペ（種別選択＋値） |
| CategoryID | `カテゴリID`(列28) | コピペ（`カテゴリ`列29 で確認） |
| Item Specifics | `Brand`(列30)〜`内容(30)`(列93) = **AE〜CP** | コピペ。空ペアはスキップ |
| Description | 共通テンプレ選択＋**英語本文を所定位置にペースト**（第7章） | ペースト |
| Selling Price | `売値($)`(列108) | コピペ |
| Quantity | `個数`(列106) | コピペ |
| Best Offer | `Best offer`(列109 ON/OFF) | **ON ならチェック → 下限値=`拒否価格`(列110) / 上限値=`承認価格`(列111) を入力** |
| Shipping Policy | `Shipping Policy`(列102) | プルダウン。**`売値($)`の価格帯→ポリシー名は配送ポリシー表で確定**（付録A-4）。シート値と一致選択 |
| Payment Policy | `Payment Policy`(列104) | プルダウン |
| Return Policy | `Return Policy`(列103) | プルダウン |
| Promoted Listings | `Promoted Listing`(列105) | プルダウン |
| Private Listing | （なし） | **常時 ON。操作不要** |

### 5-3. スペック確認（AE〜CP）
- スペックは **スプレッドシート上（AE列〜CP列 = `Brand`〜`内容(30)`）に確定値が入っている**前提で、ツールは転記のみ。
- **未確定**: 出品前にシート上で改めて確認する工程を入れるかどうかは検討中（第12章）。

### 5-4. 出品ストア（別シート「ストア名」）
- 専用シート **「ストア名」** を新設。**A列にストア名**を列挙する。
- 商品行の「出品ストア」列は、このリストから選択（データ入力規則のプルダウン推奨）。
- ツールはその値をセルスタの「出品ストア選択」プルダウンと一致させて選択する。

## 6. 利益計算機能

「利益計算」ボタンを押すと利益計算モーダルが開く。**値があるものだけ**入力する。

### 6-1. 入力マッピング
| 利益計算モーダル項目 | シート列 | 備考 |
|---|---|---|
| 販売額($) | `売値($)`(列108) | |
| 仕入価格(¥) | `仕入値(¥)`(列107) | |
| 重量(g) | `実重量(g)`(列94) | 入力して運用 |
| 高さ(cm) | `高さ(cm)`(列97) | 値がある場合のみ |
| 長さ(cm) | `奥行き(cm)`(列95) | 3辺をコピペ ※列対応は要確認 |
| 幅(cm) | `幅(cm)`(列96) | 値がある場合のみ |
| 発送方法 | `発送方法`(列101) | |
| カテゴリID | `カテゴリID`(列28) | |
| **メモ** | **`SKU`(列C) 内の「名前」部分** | 例: `売上/みゆじまさん/...` → 「みゆじまさん」を抽出して入力 |

### 6-2. 運用
- 利益計算ボタン → 重量等を入力 → 自動計算される。
- **最終的な利益の数値チェックは人が行う**可能性あり。事前にメモ欄へ数値を入れておく運用も可。
- 「値がある場合のみ反映」が原則。空欄は触らない。

## 7. コンディション・テンプレート・説明文

### 7-1. コンディション選択（シート由来の日本語→英語マッピング）
- コンディションは **スプレッドシートの `状態`(列22) から取得**（AI 判定ではなく決定論マッピング）。
- シートは日本語（新品・中古 など）、eBay は英語のため **日本語→英語の対応表で変換**。
- **シート内に eBay 公式 Condition 参照表が既にある**（付録A-3）。商品状態ドロップダウン値は
  `New / New other / Used / Very Good / Good / For parts or not working / Pre-Owned` 等。
- **注意（カテゴリ依存）**: eBay の Condition 表示名はカテゴリで変わる（例: New →
  衣類カテゴリでは "New with tags"、箱物では "New with box"）。`カテゴリ`(列29) も見て表示名を決める必要あり。
- **要定義**: `状態`(列22) の日本語値 → Condition ID（1000/3000…）の確定対応表。

### 7-2. 説明文（Description）方式 【v1.2 確定】
ユーザー提供の完成版HTML実例（`description-template-sample.html`）を正本とする。構造は**2層**。

**① 固定の枠（テンプレート・毎回ほぼ同じ）**
- 冒頭ストアリンク `Visit my store to see even more products!`（**URLのみストアごとに差替** `{{STORE_URL}}`、例 `mionastore307`）
- `Description` 見出し（青下線スタイル）
- `All my products are 100% Authentic.` / `Please feel free to contact us if you have any question.`
- `Payment`：`We usually ship within 5〜10 business days of receiving cleared payment.`
- `Shipping`：`We ship using FedEx, DHL and Japan Post.`
- `International Buyers - Please Note:` 関税の定型文

**② 商品ごとに変わる本文（＝生成プロンプトが作る唯一の部分）** ※テンプレ内の BODY 区間
1. 1文目：何の商品か＋新品/状態（例 `This is an unopened, brand new ... .`）
2. コンディション行（例 `Condition: New, unused. ...`）
3. 背景・説明の段落（数パラグラフ）
4. `It features:` ＋ 特徴の箇条書き `<ul><li>`
5. （任意）注目の引用・エピソード

**生成プロンプトの役割（上流＝ツール範囲外）**: 出力するのは **②の英語HTML断片だけ**。入力は日本語説明文 `取得`(ES列) 等。トーン＝事実ベース・状態を正直に・真贋を明記。※この生成はシート側で完結し、ツールは結果（Z列 `description`）を貼るだけ。

**組み立て〜投入の流れ**:
1. 生成プロンプトで ②本文HTML を生成
2. ①テンプレートの BODY 区間に ②を差し込み、`{{STORE_URL}}` をストア設定値に置換 → 完成HTML
3. セルスタの **CodeView（HTMLモード）に完成HTMLを貼付**（付録C-3 の Description 手順）

**テンプレート使い分けは スプレッドシート側で対応（v1.3 確定・ツール作業外）**:
- 価格（$2,500でテンプレ1/2＝米国関税文言の有無）も、発送方法（Economy/DHL・FedEx での Payment・Shipping 文言差）も、**すべてスプレッドシート側で振り分け済み**。
- ⇒ **ツールは振り分け判定を持たない**。シートが用意した「その行に対応する正しいHTML」を使うだけ。テンプレ2／Economy版HTMLの取得も**不要**。
- `description-template-sample.html` は構造把握用の実例（テンプレ1×DHL・FedEx系）として保持。

**セルスタ側の「出品テンプレート」選択（別概念）**:
- 新規出品時に**セルスタの「出品テンプレート ＋ DHL FedEx」を選択**（ストア＋発送方法の組合せ。配送ポリシー等を設定）。これは説明文HTMLとは別物。
- **出品ストア名は必ず `ストア名`(A列) の名称から選択**（ストアごとに設定が全て変わるため）。

**説明文の生成フロー（v1.4 確定・すべて上流＝シート側）**:
1. **`取得`(ES列)**: 日本語の説明文（元データ）。
2. **`英訳`(ET列)**: ESを翻訳し eBay 用にカスタマイズした**英語HTMLコード**（＝②本文）。
3. **`description`(Z列)**: ET＋テンプレートを**合体させた完成HTML**（Z列のヘッダー名は **`description`**）。← **ツールはこのZ列をそのまま CodeView に貼る**。

⇒ 結論は **(A)**: 組み立て・生成・テンプレ振り分けは**すべて上流（シート側）で完了**。
**ツールの説明文工程＝「`description`(Z列) をコピー → CodeView に貼付」だけ**。生成プロンプトはツール範囲外。

> ✅ **食い違い解消（v1.4）**: 旧 **Z列＝「コンディション説明」** は誤り。**Z列＝`description`（完成HTML）**。
> eBay の **Condition description 欄**は別物で、**中古品のときのみ** `状態テンプレート`(X列) の値→「テンプレート（英語）」で英語化して入力（付録C-3 step4）。
- → **説明文の仕様は別途、生成プロンプト設計とセットで詰める**。

## 8. エラー発生時の挙動

エラー時は基本的に **「下書き保存」状態にしたうえで**、ログを残す（詳細は付録E）。

| 記録先 | 内容 |
|---|---|
| (a) スプレッドシート B列 | `出品ステータス` に**区分**（`スキップ`/`リトライ`/`要確認`）を記録 |
| (b) スプレッドシート `出品エラー`列 | **エラーコード＋ログ**（旧 セルスタ出品/セルスタログ を統合） |
| (c) セルスタのメモ欄 | 同じエラーログを記録 |

## 9. 補助機能：シートからの画像取り込み（任意・優先度低）

- 画像はセルスタが自動読込するため**通常は操作不要**。
- 追加要望として、シート側に「画像取り込み」ボタンを置き、URL 貼付→押下で画像追加できる動線。

## 10. 連携する上流ツール（参考・疎結合）

| 上流ツール | 埋めるシート列 |
|---|---|
| ① タイトル変換ツール | `タイトル`(列20) |
| ② ディスクリプション生成ツール | eBay 用**英語説明文**（所定セル） |
| ③ スペック確認ツール | `項目名/内容(1〜30)`(列34-93) ほか AE〜CP |
| ④ 状態テンプレ・画像ツール | `状態`(列22)/`状態テンプレ`(列23)/`状態説明`(列24)/`画像*` |

## 11. 確認フロー（通し実行）

1. （自動）巡回で空欄ステータス行を検出 → `処理中` / （手動）担当者が行を指定
2. 新規出品モーダルに URL・出品ストア等を入力 →「出品画面へ」
3. 出品画面の各項目を入力（Condition は 状態→英語変換、テンプレは共通選択＋英語本文ペースト）
4. Best Offer が ON なら下限/上限を入力
5. 利益計算（重量等を入力、メモに SKU 内の名前、値があるものだけ反映）
6. **「下書き保存」** → `出品ステータス` を `下書き済`
7. 出品担当者が目視チェック → **「出品」ボタンを人が押す** → `出品済`
8. エラー時は第8章に従い `エラー`＋ログ記録

## 12. 未確定事項（次に詰める）

1. **`状態` 列の値一覧** と 日本語→eBay Condition 対応表（第7-1）
2. **共通テンプレートの選択肢一覧**（プルダウンの値）と、英語本文を貼る「所定の場所」の特定
3. **②の英語説明文の出力先セル**（どの列／セルか）
4. **Custom Link の指定セル**の場所（列）
5. **利益計算**: 「利益計算保存」ボタンを押す運用か。メモへの数値事前入力ルール
6. **3辺の列対応**: モーダルの「長さ/幅/高さ」と シートの `奥行き/幅/高さ` の対応確認
7. **リサーチ者名**(列11)のコピー先（どの項目に入れるか）
8. **エラーログ列**をシートに新設するか
9. **スペックのシート上確認工程**を入れるか
10. **自動巡回の実行基盤**: サブ PC 上で Claude Code＋Chrome をどう常駐させるか

---

# 付録A 現行シート構成（実機調査結果 / 再出品ツール）

スプレッドシート `1XNwsNA8...`（実出品用「再出品ツール」）を解析した結果。
v0.3 までの想定の多くが **既に実装済み** だった。

## A-1. メイン出品シート
- **A列が `ストア名`**（v0.3 のストア列要望は実装済み）。以降は前シートと同じ 145 列構成。

## A-2. 工程管理列（E.S.欄 = 各商品の進捗トラッカー）
`取得 | 英訳 | ディス判定 | タイトル編集 | 元タイトル | ログ | スペ確認 | 状態テンプレ | 画像確認 | 最終確認 | 出品エラー`

> v1.0: 旧 **`セルスタ出品`(FC)・`セルスタログ`(FD)** 列は**削除**し、**`出品エラー`列に統合**。本件ツールの結果は B列 `出品ステータス`（区分）＋ `出品エラー`列（コード＋ログ）に記録する。

| 列 | 担当 | 備考 |
|---|---|---|
| 取得 / 英訳 / ディス判定 | データ取得・翻訳・説明文判定 | 上流 |
| タイトル編集 / 元タイトル / ログ | ①タイトル変換ツール | プロンプト有効・設定完備 |
| スペ確認 | ③スペック確認 | 出品ツールは結果を信頼し転記 |
| 状態テンプレ / 画像確認 | ④状態・画像 | 上流 |
| 最終確認 | 人 | 目視ゲート |
| **出品エラー** | **出品ツール（本件）** | エラーコード＋実行ログ（旧 セルスタ出品(FC)/セルスタログ(FD) を統合）。区分は B列へ |

## A-3. コンディション参照表（シート内に既存）
- eBay 公式 Condition ID 表: 1000 New / 1500 New other / 1750 New with defects /
  2000 Manufacturer refurbished / 2500 Seller refurbished / 3000 Used / 4000 Very Good /
  5000 Good / 6000 Acceptable / 7000 For parts or not working。
- カテゴリ別の表示名対応表もあり（例: 1000 New → 衣類 "New with tags"、箱物 "New with box"）。
- 商品状態ドロップダウン値: New / New other / Used / Very Good / Good / For Part / Pre-Owned。
- → `状態`(列22) 日本語 → Condition ID の対応表を確定すれば、表示名はカテゴリで自動解決可能。

## A-4. 配送ポリシー表（価格帯 → ポリシー）
- `Fulfillment Policy` を価格帯で定義。例:
  - Economy 0-50 +8（ID 252682458017, 下限0/上限50, 関税8%）
  - Economy 50-100 +15 / 100-150 +23 / 150-200 +30 …
  - Expedited 150-200 / 200-250 / 250-300 / 300-350 ドル …（クーリエ）
- 各行に **ポリシーID** と 下限/上限 あり。マーケットは `EBAY_US`。
- → **`売値($)` の価格帯から Shipping Policy を自動選択**できる（決定論ルール）。
  前回の「価格でテンプレ切替(a)」案は、テンプレではなく **配送ポリシー選択** に効く仕組みとして既存。

## A-5. テンプレート（説明文HTML）
- `テンプレート1` / `テンプレート2` ＝ 完成 HTML をシートに格納。
  - 共通枠: "Visit my store to see even more products!"（ストアリンク mionastore307）、
    Description / Payment / Shipping 見出し等。
- ディスクリプション生成プロンプトは **OFF・空欄（未完成）** → 本文生成と差し込みが残課題（第7-2）。

## A-6. 画像取得（fetch_images.py）
- Playwright + Google Drive。`python3 fetch_images.py --row N` で行単位実行。
- 商品画像のみ取得 → `SKU名_画像N.jpg` で Drive 保存 → DO列（画像1）〜EK列に URL 書込み。
- **最大23枚**（eBay上限24枚内でOK）。上書き防止（DO列に値がある行はスキップ）。`画像取得ログ`シートに記録。
- 認証: `credentials.json`（OAuth）/ `token.json`（自動生成）。
- 現状の対象: メルカリ ショップス `jp.mercari.com/shops/product/` 形式。
  - **要確認**: 通常メルカリ `jp.mercari.com/item/` 形式も対象に含めるか。

## A-7. 設定・プロンプト
- 設定: タイトル文字数 下限70/上限80、型番保護 ON、サイズ反映 ON、保護ワード（I列に縦列挙）。
- タイトル編集プロンプト: 有効（before/after 例あり）。
- ディスクリプション プロンプト: OFF・空欄（未完成）。

## A-8. ストア / マーケットプレイス
- `EBAY_US`（既定）ほか EBAY_AU / ENCA / AT / BEFR / BENL / NL / CH / IE / HK / SG / MY / PH / IN / PL。

---

# 付録B 出品フロー（再出品ツール前提・更新）

1. 「出品」対象の商品を、eBay出品ツール側シートから再出品ツールの「出品」へ同期
2. （自動/手動）B列が空の対象行を選定 → B列 `出品ステータス`=`処理中` にしてロック
3. 新規出品モーダル: `ストア名`(A列)→出品ストア、URL群を入力 →「出品画面へ」
4. 出品画面入力:
   - タイトル/SKU/カスタムリンク/UPC等/カテゴリ/スペック(AE〜CP) をコピペ
   - Condition: `状態`(列22)→Condition ID→カテゴリ別表示名（A-3）
   - Shipping Policy: `売値($)`の価格帯→ポリシー（A-4）。Payment/Return/Promoted はシート値
   - Best Offer ON なら 下限=`拒否価格`/上限=`承認価格`
   - Description: 第7-2（未確定）
5. 利益計算: 重量・3辺を入力、メモに SKU 内の名前、値があるものだけ反映
6. 「下書き保存」→ B列 `出品ステータス`=`下書き保存`
7. 人が目視（最終確認）→「出品」ボタン → B列=`出品済`
8. エラー時: B列=区分（`スキップ`/`リトライ`/`要確認`）/ `出品エラー`列にコード＋ログ / セルスタのメモ欄にも記録

---

# 付録C 確定操作手順（列レター対応・実装の正本）

ユーザー指示（v0.5/v0.6）に基づく確定手順。
※⚠ は未確定・要確認（本文で別途確認中）。

> ## ★ 列の解決方法（v0.6 / 重要な設計原則）
> 列は **固定の列レターではなく、1行目ヘッダーのラベル名で解決**する
> （例:「SKU」「仕入れ先URL1」「コンディション説明」「DDP対応関税率」等）。
> **列を移動・挿入しても動作する**こと。下表の列レターは「現在位置の参考」であり、
> 実装ではヘッダー名→列インデックスを起動時に引き当てる。

## C-0. ヘッダー名 → 用途 対応表（列レターは現在位置の参考）
| 列 | ヘッダー | 用途 |
|---|---|---|
| A | ストア名 | 出品ストア選択 |
| E | 仕入れキーワード | **Custom label (SKU)** に入れる（v1.6: 旧C列SKUから変更） |
| C | SKU | （Custom labelには使わない・v1.6変更）行識別・ログ用途 |
| G | 仕入元URL① | 仕入れ先1 |
| I | 仕入元URL② | 仕入れ先2 |
| K | 仕入元URL③ | 仕入れ先3（✅確定: URL1=G, URL2=I, URL3=K） |
| L | リサーチ担当 | 利益計算メモの「名前」 |
| U | タイトル | Item title |
| W | 状態 | Condition |
| AA | ItemURL | eBay Item URL/ID（モーダル3番目の欄） |
| CQ | 実重量(g) | 利益計算 重量 |
| CR | 奥行き(cm) | 利益計算 **長さ** |
| CS | 幅(cm) | 利益計算 幅 |
| CT | 高さ(cm) | 利益計算 高さ |
| CX | 発送方法 | ✅発送方法の列。テンプレ選択ルールの判定にも使用 |
| CY | Shipping Policy | シッピングポリシー名。テンプレ出品方法の参照にも使用 |
| DE | 売値($) | Selling Price |
| DF | Best offer | ベストオファー ON/OFF |
| DG | 拒否価格 | ベストオファー オートメーション入力値 |
| DH | 承認価格 | **不要**（入力しない） |
| DI | 検索URL | カスタムリンク |
| Z | description | **完成ディスクリプションHTML**（ET英訳＋テンプレ合体済み）。CodeViewに貼る対象（第7-2）。※旧「コンディション説明」表記は誤り |
| X | 状態テンプレート | 中古品のときの **Condition description** 取り込み元（日本語キー、例「未開封」）。下記シートで英語化 |
| （別シート）| 状態　テンプレ | コンディション英文の参照表。**A列＝日本語キー / C列＝英文**。X列の値をA列で引き、一致行C列の英文をCondition descriptionに入力（例 A2「未開封」→C2） |
| FE | DDP対応関税率 | 利益計算 DDP対応関税率（v0.6訂正: 旧PEは誤り）。⚠145列範囲外＝ヘッダー名で解決 |

## C-1. 事前
- SELLSTA にログイン →「新規出品」タブを押す。

## C-2. 新規出品モーダル
1. **ストア選択**: 「新規出品」を押し、**「出品ストア選択」プルダウンに `ストア名`(A列) の値を入力/選択**（ストアごとに設定が全て変わるため、必ずA列の名称を使う）。`{{STORE_URL}}`(説明文テンプレ) も同じA列ストア名から解決。
2. **eBay Item URL/ID**（上から3つ目の欄）: AA列「ItemURL」を **フルURLのまま入力**（v1.6: IDに短縮しない）
3. **仕入れ先情報**:
   - 1つ目: G列「仕入元URL①」
   - 2つ目: I列「仕入元URL②」
   - 3つ目: K列「仕入元URL③」 ⚠（指示は J列）
4. **出品テンプレート選択**（`発送方法`(CX) で分岐・v1.5確定）:
   - 発送方法 = **エコノミー** → **エコノミー系テンプレート**（実機の汎用 `Economy`(3123)）
   - 発送方法 = **DHL または FedEx** → **DHL・FedEx系テンプレート**（実機の汎用 `DHL fedex`(3122)）
   - ※状態(新品/中古)・価格帯では分けない。汎用2択でよいか実機テストで最終確認。
5. 「出品画面へ」を押す

## C-3. 出品画面の入力
1. **タイトル**（一番上）: U列「タイトル」（編集済みタイトル）
2. **カスタムリンク**: DI列「検索URL」（仕入れ先は既に入力済み）
3. **カスタムラベル(SKU)**: E列「仕入れキーワード」（v1.6: 旧 C列「SKU」から変更）
4. **コンディション**: 「状態」(W列) を参照し該当 Condition を選択
   - 状態 = New → **Condition description 不要**
   - **中古品（Usedなど New 以外）のときのみ** Condition description を入力:
     - **X列「状態テンプレート」**の値（例「未開封」）を取得
     - **別シート「状態　テンプレ」のA列**から同じ値を探し、**一致行のC列の英文**を Condition description 欄に入力（例: A2=「未開封」→ **C2の英文**）。＝ A列(日本語キー)→C列(英文) の引き当て（VLOOKUP方式）
   - ※v1.4訂正: 旧「Z列の内容を入れる」は誤り。Z列(description)は完成HTML＝Description本体。Condition description は X列→別シート「状態　テンプレ」C列で英語化して入力。
5. **Selling Price**: DE列「売値($)」
6. **ベストオファー**: DF列が ON の場合
   - ベストオファーにチェック
   - その下の **オートメーション** にチェック → 指定スペースに DG列「拒否価格」を入力
   - DH列「承認価格」は**不要**
7. **シッピングポリシー**: CY列のポリシー名と同じものを選択
   - 無ければ新規にポリシー/テンプレートを作成して対応
8. **商品説明(Description)**:
   - 「CodeView」ボタンを押す → 編集画面（HTMLモード）
   - 既存を複製、または CodeView 上で内容を調整して貼り付け
   - 再度「CodeView」を押して戻す

## C-4. 利益計算
1. 上部の「利益計算」ボタンを押す
2. 価格は既に入っている → **仕入れ値が入っているかチェック**
3. 寸法・重量を入力:
   - 重量: CQ列「実重量」
   - 高さ: CT列「高さ(cm)」
   - 長さ: CR列「奥行き(cm)」
   - 幅: CS列「幅(cm)」
4. **メモ欄**:
   - 名前: L列「リサーチ担当」
   - エラー時: `{リサーチ担当}␣{エラーログ}`（名前の後に半角スペース1個＋エラー内容）
5. **DDP対応関税率**: 「DDP対応関税率」列（FE列）を参照し、入力箇所が空欄でなければ入力（空欄なら不要）

## C-5. 完了
- 入力が終わったら「出品画面」を押す
- 「下書き保存」を押して終了（出品ボタンは人が最終的に押す運用は維持）

## C-6. 今回不要と確定したもの
- 状態画像の取得：**不要**
- カスタムリンクの独自生成：**不要**（DI列 検索URL を使う）

---

# 付録D 成果物の保存場所・実行仕様（v0.5 指示）

## D-1. 保存場所・名称
- 保存先: **NFNFA ドライブ ＞ Company ＞ eBay ＞ eBay-listing（ebay-listing）** の中に新規フォルダを作成。
- フォルダ名・ファイル名: **「セルスタ出品」**。
- 既存の同フォルダ内容（参考・上流ツール群）:
  `credentials.json` / `description-tool/` / `fetch_images.py` / `server.log` / `server.py` /
  `setup_server.py` / `token.json` / `画像取得 仕様/`
  - ② ディスクリプション生成は `description-tool/`、④ 画像取得は `fetch_images.py` に対応。
- ⚠ 実装時に **絶対パスを確認**してから作成する（Google Drive 同期フォルダ等の可能性）。
- **実装メモ（着手）**: 仕様D-1の「Company > eBay > eBay-listing」フォルダ・上流ツール群は本Mac上で未検出
  （別アカウント/共有ドライブの可能性）。当面はプロジェクト内
  **`.claude/skills/sellstar-listing/`** にスキル本体を作成（`/sellstar-listing` で起動／日本語「セルスタ出品して」でも可）。
  構成: `SKILL.md`（操作手順の本体）/ `reference/error-codes.md` / `reference/condition-map.md`。
  Drive側へ置く場合は後でコピー。

## D-2. 実行仕様（3パターン ＋ 起動方法）

### 起動方法（トリガー）
本体スキルは1つ。**呼び出すきっかけ（トリガー）だけが異なる**。利用者がコードを書く場面は無い（打つのは合図のみ）。

| 起動方法 | 操作 | 用途 |
|---|---|---|
| ①ターミナル合図 | `claude` 起動 → `/セルスタ出品`（または日本語で「空き行を出品して」） | 手動・単発・動作確認 |
| ②ワンクリック | `.command`/エイリアスをダブルクリック → スキルを1回起動 | 手動・単発の省力化（任意） |
| ③スケジューラ | launchd/cron が3時間ごとに自動起動 | 自動巡回（無人・サブPC） |

### 3パターン（処理範囲）
| パターン | 内容 | 主な起動方法 |
|---|---|---|
| (a) 定期実行 | スケジュールで自動巡回（第2章 自動モード・3時間に1回） | ③ |
| (b) 手動実行 | 担当者が任意タイミングで実行（空き行をまとめて処理） | ①／② |
| (c) 単発実行 | 指定行を1回だけ実行 | ①／② |

### 段階導入（v1.0 確定）
1. **第1段階: ①ターミナル合図で動作確認**（最小構成。まずここから）。
2. **安定後: ③スケジューラ（自動巡回）と ②ワンクリック化を追加**。
   - 同じスキルを呼ぶだけなので、後付けしても①はそのまま使える。

## D-3. 確定した列指定（まとめ / ヘッダー名で解決）
- **列解決はヘッダー名ベース**（v0.6）。下記レターは現在位置の参考。
- 仕入れ先: URL1=「仕入れ先URL1」(G) / URL2=(I) / URL3=(K)
- 発送方法: 「発送方法」(CX)（テンプレ出品方法の判定に使用。Shipping Policy 名は CY）
- Description本体: `description`(Z列) ＝完成HTML。CodeViewにそのまま貼付
- Condition description: 中古品のみ `状態テンプレート`(X列)→ 別シート「状態　テンプレ」A列で引きC列の英文を入力

---

# 付録G 実機調査（セルスタUI実体・2026-06-08）

実際の sellsta.jp を Claude in Chrome で観察した結果。**セレクタ(ref)はセッション毎に変わるため、表示ラベルで要素を探す**こと。

## G-1. 新規出品モーダル（dialog）— 上から順
1. **出品ストア選択**（combobox）: 例 `mionastore307`(value=506)。← `ストア名`(A列)
2. **出品方式選択**（combobox）: `eBayアイテムURL / ID`(item_id) / `カテゴリーID`(category_id)。→ **`eBayアイテムURL / ID` のまま**でよい
3. **eBay Item URL / ID 入力欄**（テキスト）← `ItemURL`(AA列)
4. **仕入先1**: テキスト「ASIN、URL（楽天市場はURLのみ）」＋ ソース種別combobox（ラベル「仕入れ先情報(情報取得/在庫監視)」）
5. **仕入先2**: テキスト ＋ combobox（「仕入れ先情報(在庫監視)」）
6. **仕入先3**: テキスト ＋ combobox
   - 仕入先URL: 1=`仕入元URL①`(G) / 2=`仕入元URL②`(I) / 3=`仕入元URL③`(K)
   - ソース種別の選択肢: メルカリ(2)/メルカリショップ(11)/ヤフオク(6)/楽天市場(1)/Amazon(10)/ラクマ(3)/Yahooショッピング(9)/Yahoo!フリマ(8)/オフモール(4)/デジマート(5)/駿河屋(12)/モノタロウ(13)/有在庫(在庫監視なし)(90)/その他(在庫監視あり)(98)/その他(在庫監視なし)(99)
   - → 仕入元名（`仕入元①`F / `仕入元②`H / `仕入元③`J）でソース種別を選ぶ想定（URL貼付で自動判定される可能性あり＝要確認）
7. **出品テンプレート選択**（combobox）実際の選択肢:
   `新品 DHL`(1532) / `中古 DHL`(1536) / `新品 エコノミー 0-50ドル`(1911) / `50-100`(1912) / `100-150`(1913) / `150-200`(1914) / `中古 エコノミー 0-50ドル`(1915) / `50-100`(1916) / `100-150`(1917) / `150-200`(1918) / `販売価格 2500ドル以上 DDU`(2358) / `DHL fedex`(3122) / `Economy`(3123)
   - ⚠ 仕様C-2の「`{ストア名} FedEx DHL`」想定とは異なる。**選択ルール要確定（G-4）**。
8. **出品画面へ**（submit）

## G-2. 出品画面（「商品を出品」 /listings/:id/edit）
- 上部: 参照アイテムID / eBayアイテムID / 仕入れ先 ／ ボタン「更新」「利益計算」。
- 左カラム: **Item title**(文字数表示) / Photos & Videos / 仕入先1-3(URL+種別) / **Custom Link** / **Custom label (SKU)** /
  **Condition**(dropdown) / **Condition description**（テンプレ選択dropdown「テンプレートを選択してください」＋テキストエリア「**新品以外の場合のみ記入可能**」）/
  Item Code(UPC等) / CategoryID / Item Specifics(必須/任意)
- 右カラム: ListingType(FixedPrice) / Duration(GTC) / **Selling Price**($) / **Best Offers**(checkbox) / Quantity / Store Category /
  **Shipping Policy** / Payment Policy / Return Policy / Promoted Listings / 基本利益率ルール / 売れ残りルール / 再販ルール /
  Private Listing(checkbox) / Schedule Listing / **Description**（出品テンプレートdropdown「出品テンプレート 2 シンプル」＋リッチエディタ）
- Description エディタのツールバーに **`</>`（CodeView/HTMLソース）** ボタンあり → ここで `description`(Z列) のHTMLを貼る。
- ✅ Condition description が「新品以外のみ記入可能」＝仕様（中古のみ）と一致。
- ⚠ 保存ボタン: 既存編集画面は「**更新**」。新規作成直後は「**下書き保存**」のはず → 新規フローで要確認（G-4）。

## G-3. 利益計算モーダル
- サマリ: 利益 / 利益率 / 還付額 / 利益合計。
- 入力: 販売額($) / 送料($) / 仕入価格(¥) / その他経費(¥) / 割引額(¥) /
  **重量(g)**=`実重量` / **高さ(cm)**=`高さ` / **長さ(cm)**=`奥行き` / **幅(cm)**=`幅` / 容積重量(g) / 適用重量 /
  発送方法(dropdown 例 CPaSS DHL) / 海外送料(¥) / カテゴリーID / 落札手数料(%) /
  **メモ**=`リサーチ担当`(＋エラー時はスペース＋ログ) / 適用税率(%) / **DDP対応関税率(%)**=`DDP対応関税率`(FE)。
- ボタン: **出品画面へ** / **利益計算保存**。
- → 寸法・重量・メモ・DDPを入れ「利益計算保存」→「出品画面へ」。

## G-4. 要確定（実機差分）
1. ✅ **出品テンプレート選択ルール（v1.5確定）**: `発送方法`(CX) で分岐。エコノミー→エコノミー系（汎用`Economy`）/ DHL・FedEx→DHL・FedEx系（汎用`DHL fedex`）。
   状態・価格帯では分けない。汎用2択(`Economy`/`DHL fedex`)が正か、実機テストで最終確認。
2. 仕入先ソース種別を `仕入元名`(F/H/J) から選ぶか、URL貼付で自動判定されるか（実機テストで確認）。
3. 新規作成時の保存ボタン名（「下書き保存」か）（実機テストで確認）。

## G-5. 実データ検証メモ（再出品ツール・2026-06-08）
Drive APIで読み取り（113KB・Markdown表）。実ヘッダーと差分を確認:
- **実ヘッダー名（修正）**: `状態テンプレ`（旧「状態テンプレート」は誤り）/ `Description`（Z列・大文字D）/ 寸法は単位付き `実重量(g)`・`奥行き(cm)`・`幅(cm)`・`高さ(cm)` / 仕入先は `仕入元URL①②③`(URL)＋`仕入元①②③`(ソース種別)。
- **Shipping Policy（v1.6確定）**: `Shipping Policy`(CY)列 と同名のものをプルダウンで選ぶ。`Return`/`Payment` も同様にシート値。
  - ただし CY/Return/Payment は `ストア名` 等を参照する数式のため、**`ストア名` が空の行では `#REF!`** になる（行177がこれ）。`#REF!`/空なら **E302/`要確認`**。正常な行（ストア名あり）ではCYに値が入る。
- **`DDP対応関税率` 列は本シートに存在しない** → DDP入力スキップ（空欄扱い）。
- **`ストア名` 列はしばしば空欄**（テスト候補13件すべて空）→ 空なら現在ログイン中ストア（既定 `mionastore307`）を使うか要確認。
- 読み取りは whole-sheet（113KB・複数セクション混在で行により列ズレあり）。**本番は行スコープ読取が必要**＝Google Sheets API（OAuth creds）or ブラウザ操作。
- **シート書き戻し（`出品ステータス`/`出品エラー`）の手段が未確立**: Drive MCPは書込不可。Sheets API（creds）か ブラウザでのセル編集が必要。
- 好適なテスト行: **行177「Leica M 70周年Book」**（状態=新品/未使用＝Condition desc不要 / 発送=DHL / 売値$339 / BO ON 拒否254.6 / Description(Z)有 / ItemURL有 / 仕入元①メルカリ）。説明文サンプルと同一商品。

## G-6. 通しテスト結果（行177・2026-06-08・成功）
**結果: 新規出品→転記→利益計算→下書き保存まで成功**（listing 7460556・「下書き」バッジ／「下書きを保存しました」表示）。出品ボタンは未押下。
- 入力できた項目: ストア=mionastore307 / eBay ID=205909753847 / 仕入先=メルカリURL＋種別 / テンプレ=DHL fedex /
  Item title・画像5枚・Condition=Brand New・Item Specifics（Brand=Leica等）は **eBayクローンで自動取得** /
  Custom Link・Custom label(SKU)・Selling Price=339・Best Offers(拒否$254.6・承認は未チェック)・Description(完成HTML 6989字をCodeViewへ流し込み)・利益計算(重量1000・メモ中尾)。
- **技術メモ（実装の要点）**:
  - Description は **Summernote** エディタ。`$('#description_text_area').summernote('code', html)` で流し込み成功（CodeViewトグルは`</>`）。HTMLはbase64経由でJS注入（クリップボードはサンドボックスで不可）。
  - 仕入先ソース種別・テンプレは `<select>` を form_input で設定可。
- **要解決（重要）**:
  1. ✅ **Shipping Policy（v1.6解決）**: テンプレ任せ（誤って Expedited 450-500）をやめ、`Shipping Policy`(CY)列と同名をプルダウンで選ぶ方式に確定。行177はCY=`#REF!`（ストア名空のため）なので、この行では選べず `要確認` 扱い。正常な行では選択する。
  2. 仕入価格がモーダルで¥28900（シート`仕入値`は¥29000）。eBayクローン由来か。要確認。
  3. **シート書き戻し（出品ステータス/出品エラー）は今回未実施**（口頭報告のみ）。本番は要・書込手段（Sheets API or ブラウザ）。
- 生成物: テスト下書き listing 7460556（mionastore307・下書き）。レビュー後に削除可。
- DDP対応関税率: 「DDP対応関税率」(FE)（v0.6訂正・旧PEは誤り）

---

# 付録E エラーコード設計・回復ポリシー（v0.8）

> ユーザー確定（v0.8）: **区分（日本語）は B列・コードは出品エラー列・解説は別シート**の3層に分離。
> コードと解説を切り分けて管理（正規化）。マスタ別シートにデータを蓄積。

## E-1. ステータス列の運用（確定：3層分離）

| 場所 | 役割 | 入る値（語彙固定） |
|---|---|---|
| `出品ステータス`(B列) | 誰でも分かる**区分のみ** | （空欄=未処理）/ `処理中` / `下書き保存` / `スキップ` / `リトライ` / `要確認` |
| `出品エラー`列（FFS/工程管理内） | **具体的なエラーコード**＋ログ | `E301 …`（フォーマットはE-4） |
| 別シート「エラーコード」 | コードの**分類・解説・使い方・見方一覧**を蓄積（マスタ辞書） | E-7参照 |
| セルスタ メモ欄 | 同じログを画面側にも複写 | 〃 |

- 巡回対象は **B列が空欄** の行のみ。`処理中`/`下書き保存` は再処理しない（冪等性ロック）。
- 成功時の表示語は **`下書き保存`** に確定（ユーザー指示）。
- `ハル`（ポイント上限など全体停止）は**行のステータスではない** → B列には書かず、稼働ログ（付録F-5）に記録。※この切り分けは要確認。
- **v1.0確定**: 旧 `セルスタ出品`(FC)/`セルスタログ`(FD)列は**削除**し、**`出品エラー`列に統合**。エラーコードは一律この列に記録。

## E-2. エラーコード体系（百の位＝工程）

| 帯 | 工程 | コード例 |
|---|---|---|
| E0xx 環境・前提 | ログイン/通信/シート/列解決 | E001 未ログイン・セッション切れ / E002 通信タイムアウト / E003 シート読取失敗 / E004 ヘッダー名が見つからない（列解決失敗） |
| E1xx データ取得 | 行データ検証 | E101 必須項目が空欄 / E102 価格が数値でない・範囲外 / E103 SKU重複 / E104 カテゴリID未設定 |
| E2xx 画面遷移 | URL入力〜出品画面へ | E201 URL無効 / E202 「出品画面へ」遷移失敗 / E203 セルスタが商品情報を自動取得できない |
| E3xx 項目転記 | 各フィールド | E301 コンディション値が対応表にない / E302 該当配送ポリシーなし（価格帯外） / E303 スペック不一致 / E304 ベストオファー設定失敗 / E305 入力欄が見つからない |
| E5xx 説明文 | CodeView/HTML | E501 CodeView切替失敗 / E502 貼付元 description(Z列) が空 / E503 貼付後の検証不一致 |
| E6xx 利益計算 | 寸法・関税 | E601 寸法/重量が空（CQ/CR/CS/CT） / E602 DDP関税率の形式不正（FE列） |
| E9xx 保存・その他 | 下書き保存 | E901 保存ボタン押下失敗 / E902 保存後の検証NG / E999 未分類例外 |

## E-3. 回復ポリシー＝4区分（ユーザー確定）

| 区分（B列表示） | 意味 | ツール動作 | 代表コード |
|---|---|---|---|
| `リトライ` | 一時的（通信・タイミング） | 自動リトライ（同一行 最大N回）→超過で `要確認` | E002, E202, E501 |
| `スキップ` | 上流データ未完（まだ埋まっていない） | 今回はスキップ、次巡回で再判定 | E101, E502, E601 |
| `ハル`（全体停止） | ポイント上限・ログイン切れ等、続行不能 | 処理中の行を`下書き保存`まで進め**巡回を停止**＋通知 | E001, 残量上限 |
| `要確認` | データの誤り等、人が直す必要 | 入った所まで下書き保存して停止、人を待つ | E301, E302, E103 |

- **`スキップ`（データ未完）と `要確認`（データ不正）を必ず区別**。前者は放置で上流が埋めれば次回通過、後者は人の介入要。
- `ハル` は行単位でなく**巡回単位の停止**。B列ではなく稼働ログに記録（付録F-5）。
- **RETRY最大回数 ＝ 3回（v1.0確定）**。超過で `要確認`。

## E-4. ログ定型フォーマット

`<コード> <一言理由>（<該当列/項目>）<時刻>`
例: `E301 コンディション「美品」が対応表にない（W列 状態）2026-06-08 14:03`

## E-5. 事故防止の実装ルール（実装時に必須）

1. **冪等性**: 処理開始時に即 `処理中` を書きロック。`処理中`/`下書き済`/`出品済`は再処理しない。
2. **読み戻し検証**: 価格・SKU・コンディション等は入力後に画面値を読み直し一致確認。ズレは E3xx。
3. **極力「下書き保存」して止める**: 途中失敗でも入った分は残す（「何も残らない」を避ける）。
4. **証拠保存**: エラー時にスクリーンショット1枚を保存、ファイル名をログに記録。
5. **タイムアウト明示**: 要素待ち15秒・画面遷移30秒など上限を設定。超過は E002。
6. **ログは定型フォーマット統一**（E-4）。

## E-6. 確定事項（v1.0）と残課題

**確定（v1.0）:**
- 旧 `セルスタ出品`(FC)/`セルスタログ`(FD)列 → **削除し `出品エラー`列に統合**。
- エラーコードは一律 `出品エラー`列に記録。区分（日本語）は B列。
- RETRY最大回数 ＝ **3回**。
- `ハル` は B列に書かず稼働ログのみ（付録F-5）。

**残課題:**
- 別シート「エラーコード」の列構成の最終確定（E-7/E-8のドラフトで可か）。
- 説明文（Description）生成プロンプトのセット内容（第7-2 未完成）。

## E-7. 別シート「エラーコード」（マスタ辞書）

> 目的: コード（出品エラー列）と**解説を切り分けて管理**。出品エラー列のコードからこのシートを参照（VLOOKUP的）。データは運用しながら蓄積。

| 列 | 内容 |
|---|---|
| コード | `E301` など（主キー） |
| 区分 | `リトライ`/`スキップ`/`ハル`/`要確認` |
| 工程 | 環境/データ取得/画面遷移/項目転記/説明文/利益計算/保存 |
| 内容（見方） | このエラーが何を意味するか（一覧として参照可能に） |
| よくある原因 | 典型ケース |
| 対処方法 | オペレーターが取る手順 |
| 自動動作 | リトライ回数・スキップ等のツール挙動 |

- 「エラーコードの見方一覧」もこのシートに集約（コード欄＝索引）。
- 新しいエラーが出たら行を追加して育てる（ナレッジ蓄積）。

## E-8. 全エラーコード一覧（構成・初版）

> 別シート「エラーコード」へ転記する初版。`区分`＝B列に出る語（`ハル`は稼働ログ）。運用しながら育てる。

| コード | 区分 | 工程 | 内容（見方） | よくある原因 | 対処 |
|---|---|---|---|---|---|
| **E001** | ハル | 環境 | セルスタ未ログイン・セッション切れ | Cookie失効/ログアウト | 再ログイン後に巡回再開 |
| **E002** | リトライ | 環境 | 通信タイムアウト | 回線・サーバ遅延 | 自動リトライ→超過で要確認 |
| **E003** | リトライ | 環境 | スプレッドシート読取/書込失敗 | API一時障害/権限 | 自動リトライ→継続失敗で要確認 |
| **E004** | 要確認 | 環境 | ヘッダー名が見つからない（列解決失敗） | ヘッダー改名/削除 | ヘッダー名を仕様と突合・修正 |
| **E005** | ハル | 環境 | ポイント/利用上限に到達 | 残量枯渇 | 残量回復後に再開（付録F-6） |
| **E101** | スキップ | データ | 必須項目が空欄（タイトル/URL/価格） | 上流が未完成 | 上流入力待ち。次巡回で再判定 |
| **E102** | 要確認 | データ | 価格が数値でない・範囲外 | 入力ミス | 価格セルを修正 |
| **E103** | 要確認 | データ | SKU重複 | 既出品/重複入力 | SKU確認・修正 |
| **E104** | スキップ | データ | カテゴリID未設定 | 上流未完成 | カテゴリ確定待ち |
| **E201** | 要確認 | 画面遷移 | eBay/仕入先URLが無効 | URL誤り/失効 | URLを修正 |
| **E202** | リトライ | 画面遷移 | 「出品画面へ」遷移失敗 | 一時的UI/通信 | 自動リトライ→超過で要確認 |
| **E203** | リトライ | 画面遷移 | セルスタが商品情報を自動取得できない | 取得元一時障害 | リトライ→継続失敗で要確認 |
| **E301** | 要確認 | 項目転記 | コンディション値が対応表にない | 想定外の状態語 | 対応表に追加 or 値修正 |
| **E302** | 要確認 | 項目転記 | 該当する配送ポリシーがない（価格帯外） | 価格帯の穴 | 配送ポリシー表を見直し |
| **E303** | 要確認 | 項目転記 | スペック不一致（Item Specifics） | シートと画面の差異 | 該当項目を確認・修正 |
| **E304** | リトライ | 項目転記 | ベストオファー/拒否価格 設定失敗 | UI一時不全 | リトライ→超過で要確認 |
| **E305** | リトライ | 項目転記 | 入力欄が見つからない（UI変化） | セルスタUI変更 | リトライ→継続失敗で要確認（UI追従要） |
| **E501** | リトライ | 説明文 | CodeView切替失敗 | エディタ一時不全 | リトライ→超過で要確認 |
| **E502** | スキップ | 説明文 | 貼付元 `description`(Z列) が空（完成HTML未生成） | 上流未完成 | Z列 description 生成待ち |
| **E503** | リトライ | 説明文 | HTML貼付後の検証不一致 | 貼付欠落 | 再貼付リトライ→超過で要確認 |
| **E601** | スキップ | 利益計算 | 寸法/重量が空（CQ/CR/CS/CT） | 上流未完成 | 寸法・重量入力待ち |
| **E602** | 要確認 | 利益計算 | DDP対応関税率の形式不正（FE列） | 入力ミス | FE列を修正 |
| **E901** | リトライ | 保存 | 下書き保存ボタン押下失敗 | UI一時不全 | リトライ→超過で要確認 |
| **E902** | リトライ | 保存 | 保存後の検証NG（下書きにならない） | 保存未反映 | リトライ→継続失敗で要確認 |
| **E999** | 要確認 | その他 | 未分類の例外 | 想定外 | ログ/スクショで個別調査 |

---

# 付録F サブPC・複数端末運用（v0.8）

> 前提: メインPC＝手動操作 / サブPC＝24h自動巡回（3時間に1回程度）。台数を増やしても安全に動く設計。
> **2つのキモ: ①2台が同じ行を奪い合わない ②サブPCが止まっても気づける。**

## F-1. 二重処理の防止（シート側ロックが正本）
- 行を拾ったら**最初に即 B列を `処理中`** に書く＝ロック宣言。空欄行しか拾わないので先勝ち。
- **`担当端末`列**（例 `SUB-PC`/`MAIN-PC`）を新設し、どの端末が処理したか残す。
- ロック直後に読み戻し、自分の端末名か確認（競合検知）。ズレていたら譲る。
- ※端末側の排他ではなく**シート側の状態**で排他するのが要点（台数非依存）。

## F-2. 各端末に必要なもの（セットアップ）
- Claude Code（CLI）＝スキル実行本体
- Chrome ＋ Claude in Chrome 拡張（**ヘッドレス不可**：実ブラウザ＋拡張接続が必要）
- セルスタにログイン済みの Chrome プロファイル（Cookie）
- Google 認証 `credentials.json`/`token.json`（**トークンは端末ごとに別**。共有しない）
- スキル本体は共有Drive「セルスタ出品」フォルダ等に置き、**両端末が同一バージョン**を参照

## F-3. 定期実行（巡回）
- 確定: **「1起動＝1巡回（空欄行を順に処理して終了）」の単発実行を、スケジューラで3時間に1回 起動**（v0.9）。頻度は後から調整可。
- 起動手段: Claude Code のスケジュール実行、または OS スケジューラ（macOS launchd / cron）から `claude` 起動。
- 常駐ループより毎回まっさら起動の方が事故りにくい。

## F-4. 端末が止まらない工夫
- スリープ無効化（macOS `caffeinate` / 電源設定）。
- 画面ロック時に Claude in Chrome 操作が止まらないか事前テスト（前面Chrome必須のため）。
- セッション切れ時は `E001` → `ハル`（巡回停止）＋通知。失敗を続けさせない。

## F-5. 監視・通知（無人運用の生命線）
- **ハートビート**: 別シート「稼働ログ」に巡回ごと `端末名 / 時刻 / 処理件数 / 結果` を1行追記。最終時刻で生死判定。
- **HALT通知**: `ハル`（ポイント上限・ログイン切れ等）でメール/プッシュ通知。
- 連続失敗（例: 5行連続 `要確認`）でも巡回停止＋通知（設定ミスの暴走防止）。

## F-6. ポイント上限（ハル）への備え
- ハル時は処理中の行を `下書き保存` まで進めてから停止（中途半端を残さない）。
- 稼働ログに `ハル理由` と `最後に処理したSKU` を記録 → 翌日メインPCが続きから再開可能。

---

# 付録G-7 通しテスト2（行6 Kate Spade・中古・2026-06-08・成功）＋重要訂正
**結果**: 中古品の通しも成功（listing 7461223・下書き）。Condition description（中古の英文）まで含めて完走。
- 入力: ストア mionastore307 / ItemURL=フルURL(236690655783) / 仕入先3組(メルカリ・Yahoo!フリマ・メルカリ) / テンプレ DHL fedex /
  Custom label(SKU)=`仕入れキーワード`「Kate Spade 2WAY shoulder bag green」/ Selling Price=250 / Best Offer 拒否180.9 /
  Condition=Pre-owned-Good(クローン自動) / Condition description=英文 / **Shipping Policy=Expedited 200-250ドル(CY値・価格帯一致)** /
  Description=完成HTML(7700字) / 利益計算(重量500・高さ23・長さ15・幅31・メモ コンノさん)。

**重要訂正（実シート確認で判明、G-5/G-6の一部を上書き）**:
1. **シート構造**: 1〜4行目ヘッダー、**データは5行目〜**。行5=Leica(メモ"例")、行6=Kate Spade。`ストア名`は実際入っている(mionastore307)。
2. 以前の「ストア名が空 / CY=#REF! / Shipping Policy不一致」は **Drive APIエクスポートの列ズレ(-2列)による誤読**だった。実機のCYは `Expedited 200-250ドル` 等の有効値。よって G-4#1・G-5・G-6#1 の#REF!前提は撤回。
3. **Condition description の取り込みキー**: 実機では `状態テンプレ`(X)は空が多く、**`状態`(W)** に日本語語句（例「目立った傷や汚れなし」）が入る。**X→無ければW** の語句で「状態　テンプレ」シートA列を引きC列英文を入力。
4. **シート読取**: Drive APIエクスポートは複数タブ連結＋列ズレ＋切り詰めで精密抽出に不向き。**本番は Google Sheets API（行スコープ読取）必須**。
- 生成物: テスト下書き listing 7460556(Leica)/7461223(Kate Spade)。レビュー後に削除可。

---

# 付録H シート連携（Google Sheets API・確定 2026-06-08）
**読み書きはブラウザでなく Google Sheets API（gspread＋サービスアカウント）に確定。**
- 既存資産を再利用: `/Users/nfnfa/Desktop/credentials.json/`（サービスアカウントcredentials.json＋gspread入りvenv）。
- サービスアカウント: `naka31@enduring-stage-496617-n5.iam.gserviceaccount.com`。
  → 再出品ツールを**このアカウントに「編集者」共有**（2026-06-08実施）。読取・書込とも検証OK。
- スキル内ヘルパー **`.claude/skills/sellstar-listing/sheet_io.py`**:
  - `pending`（出品ステータス空＆ItemURL有り行）/ `read <行>` / `status <行> <値>` / `error <行> <ログ>`。
  - 列はヘッダー名解決。読取は列ズレ・切り詰めなし、日本語書込も確実（ブラウザtypeの不安定を回避）。
- これで本番の通し（pending→ロック`処理中`→Chromeで下書き→`下書き保存`/エラー書戻し）が成立。
- 自動巡回（3時間ごと）＋ワンクリックは段階導入の第2段階。

## H-2. 完全ループ実演（2026-06-08）
- **成功パス**: 行5(Leica/新品)・行6(Kate Spade/中古) → 下書き保存＋`status=下書き保存`書戻し（API）。
- **エラーパス**: 行7(Studio Clip/中古) → read→`処理中`ロック→セルスタ操作→**Shipping Policy必須エラー**（CY空で保存不可）→`status=要確認`＋`出品エラー=E302…` 書戻し。
- **確定事項**:
  - **Shipping Policy はセルスタの下書き保存で必須**。**CY空/#REF! のとき（v1.7・ユーザー方針）は、売値の価格帯に合う暫定ポリシーを選んで下書き保存し、status=要確認＋E302を記録**（人が最終確認で正ポリシーに修正）。ブロックしない。
  - タイトル等の「ブラックリスト：◯◯」（商標ワード）警告行も要確認に含める。
  - 追加実演: 行7(Studio Clip/CY空)→暫定Economy100-150+23で下書き保存＋要確認。行8(TENSHODO Ring/発送=Economy/CY=Economy100-150+23)→下書き保存（成功）。
  - Condition description は `状態`(W)の語句で「状態_テンプレ」シート(A→C)を引く。該当語が無い値（例「中古品」）はマッピング無し＝空のまま（要確認ではない／テンプレ拡充の余地）。
- → 成功・エラー両パスの通しを実機検証済み。手動モードは本番投入可能。
